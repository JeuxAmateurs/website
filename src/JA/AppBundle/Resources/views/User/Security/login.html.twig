{% extends 'JAAppBundle::layout.html.twig' %}

{% trans_default_domain 'FOSUserBundle' %}

{% block title %}Sign in - {{ parent() }}{% endblock %}

{% block body %}
    <div class="block">
        {% if error %}
            <div>{{ error.messageKey|trans(error.messageData, 
'security') }}</div>
        {% endif %}

        {% include 'JAAppBundle:Forms:login.html.twig' %}

        {#<a href="{{ path('fos_user_change_password') }}">
            {{ 'change_password.forgotten'|trans }}
        </a>#} {# @todo: Activate this after setting mails #}
    </div>
{% endblock %}
