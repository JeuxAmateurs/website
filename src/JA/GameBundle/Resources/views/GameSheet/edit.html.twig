{% extends "JAGameBundle::layout.html.twig" %}

{% block title %} {{ parent() }} - Edit {{ entity.projectName }} {% endblock %}

{% block content %}
	<h3>GameSheet edit</h3>

	<form action="{{ path('gamesheet_update', { 'id': entity.id }) }}" method="post" {{ form_enctype(edit_form) }}>
		{{ form_widget(edit_form) }}
		<p>
			<button type="submit">Edit</button>
		</p>
	</form>

	<ul class="record_actions">
		<li>
			<a href="{{ path('gamesheet') }}">
				Back to the list
			</a>
		</li>
		<li>
			<form action="{{ path('gamesheet_delete', { 'id': entity.id }) }}" method="post">
				{{ form_widget(delete_form) }}
				<button type="submit">Delete</button>
			</form>
		</li>
	</ul>
{% endblock %}